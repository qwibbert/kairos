{
  "version": "0.6.2",
  "devDependencies": {},
  "scripts": {
    "commit": "cz",
    "build": "docker build .",
    "prod": "cd frontend && npm run build && rm -rf ../backend/pb_public/* && cp -R build/* ../backend/pb_public && cd ../backend && go build . && ./kairos serve"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "@simple-release/npm": "^2.3.0"
  }
}
